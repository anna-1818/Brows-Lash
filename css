/* Мобильные стили */
@media (max-width: 768px) {
    .container {
        width: 95%; /* Ширина контейнера для мобильных устройств */
    }

    header {
        padding: 10px 0; /* Уменьшение отступов в заголовке */
    }

    .route-details {
        padding: 10px; /* Уменьшение отступов в секции маршрутов */
    }

    .button {
        width: 100%; /* Кнопка занимает всю ширину */
        text-align: center; /* Центрирование текста в кнопке */
        padding: 15px; /* Увеличение отступов для кнопки */
    }

    .route-details img {
        width: 100%; /* Изображение занимает всю ширину контейнера */
        height: auto; /* Автоматическая высота для сохранения пропорций */
    }

    .oval-image {
        width: 80px; /* Уменьшение ширины овального изображения */
        height: 80px; /* Уменьшение высоты овального изображения */
    }
}

body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #E0F7FA; /* Нежно-голубой цвет фона */
}

footer {
    background-color: #E0F7FA;
    padding: 20px 0;
}

footer .container {
    display: flex;
    flex-direction: column; /* Измените на column для мобильных устройств */
    align-items: center; /* Центрируем элементы */
}

.contact-info {
    margin-bottom: 20px; /* Отступ снизу для контактной информации */
}

.contact-form-wrapper {
    width: 100%;
    max-width: 600px; /* Ограничение ширины формы */
}

.contact-form {
    display: flex;
    flex-direction: column; /* Вертикальное выравнивание полей */
}

.contact-form .form-group {
    margin-bottom: 15px;
}

.contact-form .form-group label {
    display: block;
    margin-bottom: 5px;
}

.contact-form .form-group input,
.contact-form .form-group textarea {
    width: 100%; /* Поля занимают 100% ширины */
    padding: 10px;
    box-sizing: border-box; /* Корректное отображение с padding */
}

.contact-form .button {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 10px 20px;
    cursor: pointer;
}

.contact-form .button:hover {
    background-color: #0056b3;
}

.copyright-bar {
    text-align: center;
    margin-top: 20px;
}
